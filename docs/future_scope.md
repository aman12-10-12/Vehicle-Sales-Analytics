# Future Scope

- **Hyperparameter tuning** with `RandomizedSearchCV` on Random Forest
- **SHAP explainability** for individual price predictions
- **Streaming pipeline** (Kafka) for daily auction ingest (Master Q12)
- **dbt-style ELT** with staging → marts layers and data contracts (Master Q8)
- **Automated data quality alerts** when null % or duplicate % exceeds thresholds
- **API endpoint** wrapping `predict.py` for real-time dealer quotes
- **Full Power BI deployment** to Power BI Service with scheduled refresh
